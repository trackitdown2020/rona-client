import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(theme => ({
  container: {
    
  },
  image: {

  },
  title: {
    fontSize="20sp"
  },
  description: {
    fontSize="12sp"
  }
}));

export { useStyles };